#define COLS 7
#define SYMBOL '*'
#define 

bool checkConstDiag (char board[][COLS], int lRow, int lCol){
    for(int r=3; r<lRow; r++){
        for(int c=0; c<4; c++){
            if(board[r][c] == )
        }
    }
}